#include "BriefingManager.h"

void BriefingManager::openOrUpdateBriefer(bool zero) {
	// if (m_briefing && NsBrieferMovieController::s_instance)
	// 	NsBrieferMovieController::s_instance->openOrUpdateBriefer(zero);
}

bool BriefingManager::detectDev() const {
	// if (m_briefing && m_movieController)
	// 	return m_movieController->detectDev();
	return false;
}
